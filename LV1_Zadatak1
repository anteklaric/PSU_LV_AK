#prvi zadatak

radni_sati=int(input())
placa_sat=float(input())

total=float(radni_sati*placa_sat)
print(total)

def total_euro(radni_sati, placa_sat):
    print(radni_sati*placa_sat)

total_euro(radni_sati, placa_sat)
